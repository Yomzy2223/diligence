export interface cmFieldPropType {
  form: any;
  name: string;
  label?: string;
  tipText?: string;
  type?: string;
  placeholder?: string;
  textSize?: string;
  bottom?: any;
  defaultValue?: string | number;
  classNames?: {
    formItem: string;
  };
  disabled?: boolean;
}
